# GCP-specific dependencies for Mycelium
# Install with: pip install -r deploy/gcp/requirements-gcp.txt

# Vertex AI SDK (Gemini + Embeddings)
google-cloud-aiplatform>=1.38.0

# Cloud SQL connector
cloud-sql-python-connector[pg8000]>=1.5.0

# PostgreSQL driver
pg8000>=1.30.0

# SQLAlchemy for connection pooling
sqlalchemy>=2.0.0

# Google Cloud authentication
google-auth>=2.23.0
