# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.coverage

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# uv
.uv/
uv.lock

# Scratch / local-only
ideas.md
notes/
results/
logs/
*.log
experiments/
secrets/
untracked/
findings/
# docs/ tracks design specs and archived pre-v98 content (see docs/archive/)

# Data files: ignore-all, then re-include named exceptions (ORDER MATTERS)
*.json
!.beads/*.json
!deduplicated_templates.json
!package.json
*.jsonl
!.beads/*.jsonl
*.yaml
!.beads/*.yaml
!configs/*.yaml
*.db
*.db-shm
*.db-wal
*.sqlite
*.backup.*
*.db.backup*
*.centroid_cache.npz

# Beads runtime artifacts (MUST stay after !.beads/*.json)
.beads/backup/
.beads/embeddeddolt/
.beads/export-state.json

# Checkpoints and large binary files
checkpoints/
.cache/
*.safetensors
*.tar.gz
*.pt
*.bin

# Lambda build artifacts
lambda/*/lambda.zip
lambda/*/package/

# Shell scripts (keep scripts/ — project utilities; ORDER MATTERS)
*.sh
!scripts/*.sh

# Claude Code local memory
.claude/
