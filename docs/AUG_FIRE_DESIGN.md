# THE AUGMENTATION FIRE — FIRE-READY DESIGN
*(2026-08-01, staged cold. The prerequisite fire: the surface-band
law's cure attempt. Ignition on the word; nothing burns here.)*

## The claim under test
Surface variety in training dissolves (part of) the band — measured as
paraphrase invariance gained, slot emission gained, and the two
standing before-fixtures moving. NOT "more variety helps": the sink
framing — is the table's variance enough to absorb, or does the
boundary merely relocate?

## The arms (per THE SINK-DIMENSION PIN: table variance is the axis)
Base: gen22_mix. Augmentation = re-render a fraction of base rows
through the licensed table (graph fixed, gold preserved — rows are
RE-RENDERED, never new; the check is exact membership).
- **V-LOW**: renders drawn from HALF the table's entries per family
  (stratified; the other half held out of training entirely).
- **V-FULL**: renders drawn from the full 41-entry table.
- Row-fraction FIXED at one value both arms (10% of base re-rendered;
  the dup fire showed fraction is not the lever in reach).
- DRY only (the wetting call rides the dup ordering separately).
Two arms, one axis, the variance question isolated. (A row-fraction
arm may be added by word if V-FULL moves the bars — crossed later,
never marginally read.)

## The after-read (all pinned already)
1. **Flip rate** on the wild ledger's transformed set: primary < 40%
   (from 53.1%), strong < 27%.
2. **Mouth-native share** on the 1404: rises above 1.6%.
3. **The before-fixtures**: dup held-out-config 24% and fdiv
   varied-surface 0/8 — must move, measured with HELD-OUT-OF-TABLE
   renderings only (the recursion guard; V-LOW's held-out half
   supplies additional eval renderings no arm trained on).
4. **Slot emission** beside binding correctness, separately, per the
   parseability rider.
5. **Per-family effects** incl. THE MUL PREDICTION (68% coverage
   predicts smaller mul effect; equal-to-pct refutes
   coverage-as-mechanism).
6. In-register floors: bigtest within band, cert-v2 ≥ 0.998.

## Recipe
Corpus: re-render via table (CPU mint, staged: assemble → verify
membership → one atomic mix write). Fire: gentle continuation from
g22, fire_gen22 shape (4×4k, LR 1e-4, states assembled per-arm with
sentinels, one arm resident — the disk law). g22 remains the gate
until a battery verdict prints PROMOTED. After-reads per arm, then
the standing rehearsal both tiers (entourage duty if promotion).

## Cost
2 arms ≈ 2.5h GPU (mint CPU; assembly from banked base states + fresh
rendered-row states; the staged-I/O shape throughout).
