# THE SCHEMA FLOOR SPEC — the waist's resolver (2026-08-10;
# spec-first; nothing here authorizes a fire)

**Summons (measured, four reads deep):** census → families →
stations → waist: bigtest's 245-miss tail is subsurface-saturated
(100%), fails at the pointers (66%), and the pointer probe printed
ABSENT (0.185 vs 0.983) — the ordinal/inverse indirection is never
resolved into the waist. The floor is the tail's ONLY road. Prior
summons stand beneath it: the census's 83% schema-altitude bar,
#169's orientation metadata, the clock's boundary dependency.

## 1. The artifact-or-altitude question — ANSWERED BY THE RECORD
#182 asked: is the floor a committed artifact the parse consumes,
or an altitude the loop refines? THE WAIST-LIMITED FINDING PICKS A
THIRD FORM THE DICHOTOMY MISSED: **the floor is SUPERVISION** —
gold-side altitude, artifact unchanged. The parse's output stays
the typed graph (the only thing the two-jaws law lets certify);
what changes is that the WAIST is TAUGHT to resolve indirection
(auxiliary prediction, the C1-A receipt: auxiliary beats hint),
so the evidence the pointers need becomes PRESENT in the state.
No runtime artifact, no loop stage, no text markers — gold.

## 2. The design (E-FLOOR, first form)
**Auxiliary referent supervision:** for every rel argument whose
surface is INDIRECT (ordinal "the fourth number", inverse frames
"exceeds b by", role words), an auxiliary target trains the slot
to name the RESOLVED variable. Gold is derivable AT MINT — the
generator knows which var every ordinal/role denotes (zero
annotation cost; the mint's own mapping). Implementation shape:
a per-slot auxiliary pointer target (reuse the args gold pathway
on an auxiliary emission), weight ≪1 (the −107 lesson), dose as
a declared sliver of indirection-heavy rows in the carrier.

## 3. Fences (inherited, in from birth)
teaching-not-telling (supervision, never text hints) · Goodhart
(no diagnostic in loss; the probe stays audit-side) · the REFOLD
LAW (any training moves the waist — the dup fold re-derives after,
by rite) · grain conversions + overlap-before-pricing on all bars ·
neighbor reads (sentinels: sgn, sel, dup cells post-refold) ·
the mint-schema contract (era.py).

## 4. First customer + bars (numbers at fire registration)
The 245-row tail. THE VERIFICATION ORDER IS THE FAMILY PLAYBOOK
REVERSED: the pointer probe RE-RUNS FIRST — **0.185 must move
toward presence** (the mechanism at the waist, before any score);
then pointer/station reads; then bigtest (>1238 is the prize,
1233 the kill floor). A score move without the probe moving =
wrong-reason, refused. Second customer: the filler species'
orientation metadata (#169's original constituency).

## 5. Order of work
spec (this) → gold-derivation check (can the mint label every
indirect mention? zero-GPU, the schema contract) → E-floor fire
(arm + control, from g23v5, h_dup refolded after per the law) →
probe-first battery. Patience is the schedule; the organs are
the plan.
