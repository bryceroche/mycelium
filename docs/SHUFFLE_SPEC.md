# SHUFFLE_SPEC — the shuffle organ (2026-08-15, word given; bars before fire)

**The disease it targets:** binding collapse at the read — ~30% factor
substitution on the refusal-65 (presence intact, query perfect, routing
wrong; long-range at deep dose: 72 non-adjacent vs 18 adjacent arg subs).
**The address:** between MAP (works: 996/997) and REDUCE (sound: honest
refusal) — the routing stage. The only cure receipt of the arc lives at
this address in text space (len_asc 159/233).

## THE DECLINE-PROOF ANSWER (the spec's load-bearing clause)

#62 was declined because structure was OFFERED — a learnable gate priced
by the aggregate gradient, where the easy majority's small perturbation
cost outvotes the tail's benefit. The campaign's actual structural wins
were never offered; they were IMPOSED: the slot banks, consecutive
letters, tokmask, sent_emb, the breath slot_mask — wiring, not knobs.
**This organ contains NO learnable gate and NO loss term. It cannot be
declined because there is nothing to decline: rungs A0-A1 involve ZERO
training — the shuffle is a symbolic step at inference.**

Majority-safety is by construction, not by hope: solved rows already
route cleanly (fat overlap 0.027 — near-orthogonal); a 1:1 matching
reproduces their existing assignment and the mask merely formalizes it.
The cost to the majority is measured as a pinned hold bar, not assumed.

## THE MECHANISM: neural proposes, symbolic disposes — at the binding

Two passes, one matching step between them (the two-forward economics:
Nazaré's bolt-on precedent — structure carries; training-under consumes):

1. **PASS 1 (propose):** standard forward. The fat attention matrix IS
   the affinity table: slot j's mass over sentence s = affinity(j,s).
2. **THE SHUFFLE (dispose):** discrete bipartite matching — each GIVEN
   slot assigned exactly one sentence, maximizing total affinity under a
   uniqueness constraint (greedy or Hungarian; collisions — two slots
   claiming one sentence, the measured overlap signature — get REROUTED
   by the matching instead of resolved by whoever shouts loudest).
3. **PASS 2 (read under routes):** re-forward with the factor bank's
   scores masked per slot to its assigned sentence — riding the pbias
   hook #62 already built (0 for allowed tokens, -1e9 elsewhere; a mask,
   not a bias — no gradient, no gate, no veto surface).

**Six-wave's place:** the phase alphabet enters the MATCCHING as the
tie-break/ordering cost (antiphase preference between adjacent slots),
and FDM x TDM returns as matching-per-settle-round when concurrent
facts exceed channels. It rides the shuffle; it is not the shuffle.
Its contribution is barred separately (A2) — the thesis's two halves
get separate receipts or separate graves.

## RIVETS (carried, per the countersign)
- The June engine is MOTIVATION, not receipt (different substrate,
  different axis — the banked-sweep rule, third application).
- **The learned in-model shuffle has NO receipt. Every in-model delivery
  of this family was vetoed or inert.** Rungs A0-A1 are zero-training
  precisely so the organ can earn its first in-model receipt without
  touching an optimizer. Rung C (training under imposed routing) exists
  only behind an A-rung receipt + its own registration.

## THE LADDER (bars pinned at registration, before each fire)
- **A0 (organ smoke; zero training):** gate ckpt, two-pass matching-mask
  read on residue-74 + 240-row solved sample. Bars: solved hold >=0.98;
  residue-74 conversions reported with an honest floor (>=5 = the organ
  touches the untouchable fixture; 0-4 = report, no claim). fat_overlap
  drops mechanically under masks — REPORTED, never claimed as cure.
- **A1 (census):** full 233 + bigtest straight + scan under the organ.
  Bars: bigtest net non-negative; scan no cell worse.
- **A2 (six-wave rider):** matching WITH vs WITHOUT phase cost, same
  rows. Bar: phase-cost matching converts strictly more than plain.
- **B (rel-args routing):** extend routing to rel argument pointers
  (the 196 missing rels). Registration after A verdicts.
- **C (the trained form):** only behind an A receipt; own registration;
  the relocation law governs.

## FENCES
Solver untouched. No loss terms, no supervised diagnostics (fat_overlap
stays coupling-NEVER). Deployment-honest: pass-1 assignment uses the
model's own fat — no gold at any stage. The mouth's register check runs
before the organ (OOD rows refuse before they route).
