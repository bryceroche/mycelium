# PLAN DRAFT — next steps (2026-08-08; GOVERNING FRAME RATIFIED:
# build the whole kitchen, patiently — Cambium and the Cathedral.
# The model cannot cook holding every ingredient: breath cycles +
# REPLACE/ACCUMULATE notebooks + the happy-families test (the
# canonicalizer) + convergence across breaths + the Nazaré commit
# wave. Phases below serve THAT architecture, not quick cures.)

## Phase 1 — ORGAN 3: THE CLOCK (the board's own conclusion)
Thrice-summoned by measurement (double-failure branch, arm 1's timing
flip, the exit's can't-reach-WHEN). Order of work:
1. **Spec** (CLOCK_SPEC.md, spec-first like the notebook): the WHEN
   organ. First design question already pinned — THE NAZARÉ OPERATOR
   (constraint count vs slot degree vs bound-arg fraction). Fences
   in from birth: phase touches memory/commit ordering never
   deduction; register clause (commit trigger = own trained head,
   gold from solver-verified finality; settle never).
2. **Operator scan BEFORE building** (cheap GPU): compute the
   candidate operators on banked parses — constraint count, slot
   degree, bound-arg fraction; NOTE the register-clause fence:
   cross-breath convergence is GOLD-SIDE only (finality labels),
   never an inference-time trigger input; which best separates
   healthy commits (nd=0 correct) from premature ones (nd=1, the
   tok-21 rows)? Pick the operator by measurement, not flattery.
3. **Build clock v1**: commit gating conditioned on the chosen
   operator (rings pawl machinery reused; one-cut, init-closed).
4. **Fire** on inherited bars: nd=1 <8/15; verification = HELD AND
   WON at token grain; no-harm nd=0; bigtest −5; kill >5.

## Phase 2 — THE THREE-LAYER DUP CURE (after clock v1)
Assemble: STAGE (clock) + WIN (preference/operand supervision at
weight ~0.05–0.1, not 1.0 — the −107 lesson) + EXIT (beam exit,
kept as rings' repair). One registration, three tools, the standing
bars. This is the premature-commitment family's full cure candidate
— if it lands, it plausibly reaches the filler species too (same
root, spatial grain).

## Phase 3 — COVERAGE UNDER THE CLAIM (parallel-capable, your call)
- **Schema floor** (in-register bevel): orientation-metadata
  supervision (#169; auxiliary prediction, C1-A receipt). Honest
  scope: hones the bevel where growth is lawful.
- **E1+E3 answer-space** (sign+width): design work, raises
  addressable 39.4%→~63%; gen-23 shipped sign; width redesign =
  copy-not-generate successor (its own registration).
- **Books cadence**: the standing dialect harvest, unchanged.

## STANDING / GATED (no action)
Mouth-widening (four-bevel precondition) · marker relaxation +
steering revisit (same trigger) · deep-tail claim (second shape) ·
arms A/B — PROPOSED KILL: superseded by the three-layer map
(anchor-bias steering's questions absorbed into the cure brief).

## Decision points for the word
(1) Ratify clock-first ordering? (2) Operator-scan-before-build
discipline? (3) Schema floor: parallel with Phase 1, or after?
(4) Approve the arms A/B kill? (5) Anything here you'd strike?
