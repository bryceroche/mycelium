# SIXWAVE_SPEC — six helical sine waves as main driver (2026-08-15)

**The claim:** binding-under-crowding is crosstalk+dispersion on a shared
line (the telegraph synthesis, ledger 2026-08-15); the cure class is
CARRIER ASSIGNMENT — phase-distinct channels per sentence/slot so adjacent
signals never share a phase. Six = 3 antiphase pairs covering the measured
6-9 sentence erosion band with guaranteed antiphase adjacency;
sum-cos^2=3 (no deadband), pairs (k,k+3) cancel common-mode.

**Constitutional fences:** carrier enters as STRUCTURE (input feature),
never as loss (door #61 priced supervision-on-shared-canvas: -94);
zero-init gate (V11 receipt) + gentle dose (overcorrection law); solver
untouched; the crowding scan stays diagnostic (Goodhart fence).

## The ladder (each rung bars-first)
- **Rung 0 (zero-GPU, banked states):** deadband-in-representation read.
  Metric: mean cos(adjacent middle-sentence mean-states) minus mean
  cos(non-adjacent), per row; populations solved-sample / converted-159 /
  residue-74. REGISTERED PREDICTION: miss populations show HIGHER adjacent
  similarity (carrier collapse); if populations are indistinguishable the
  carrier has no measured target and Rung 1's warrant weakens (pinned).
- **Rung 1 (the carrier fire):** helical carrier c(t)=A[sin(w*pos+phi_s),
  cos(w*pos+phi_s)], phi_s=pi*(s mod 6)/3, added to the sentence channel
  through a ZERO-INIT learned gate; gate-warm, retirement carrier recipe.
  Bars: dup_axis_scan curve (0/3/9/12) improves >=1 cell; residue-74
  conversions reported; bigtest within ring; sight held. Kill: scan flat.
- **Rung 2:** slot-bank phasing (slot k -> phase k mod 6) — carrier
  assignment at the decode end (neighbor-copy is slot-adjacent).
- **Rung 3 (organ arc):** anchors as repeaters at six breath-cycle phases.
