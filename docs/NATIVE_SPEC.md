# NATIVE_SPEC — the Grand Vision from birth (2026-08-15, the word given)

**The hill:** six-wave carriers + shuffle + breath message-passing as NATIVE
architecture — present from step 0, so the receiver learns the phones from
birth (the two-phone law's untried clause; #55 was mid-life integration,
this is not that).

**V1 components (native arm):** six-wave resonance in the factor bank
(ALG_SIXWAVE, gate free to grow from birth — no converged majority exists
yet to veto it); breath cycle on from birth (ALG_BREATH=3 + BREATH_NORM
ladder). **V2 (spec'd, not in this fire):** the letter-keyed soft partition
(lsent input: var slot v biased toward sentences mentioning letter v —
regex-derivable at train AND inference; the shuffle's two-phone form).

**THE TWIN PROTOCOL (the from-birth control):** two heads, SAME seed, SAME
diet (form_mix8 + rations), SAME steps (4000), RANDOM INIT both (no
WARM_FROM — warm-starting would be mid-life again). Native arm carries the
structures; twin carries none. Every meter reads as native-minus-twin.

**BARS (pinned):** (1) native trains stably (finite loss, sight > 0);
(2) ENGAGEMENT: sw_g grows |>0.05| from birth OR the claim "no majority
veto at birth" is refuted; (3) DIFFERENTIAL: native beats twin on >=2 of
{val, crowding scan (>=1 cell, none worse), fat_overlap on the matched
fixture}; KILL: no differential anywhere = the two-phone hypothesis loses
its from-birth clause and the hill is surrendered honestly. Neither arm
is ring-competitive at 4000 steps from random — NOT a bar, stated now.
