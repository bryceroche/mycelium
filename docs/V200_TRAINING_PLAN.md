# V200 TRAINING PLAN (draft for the gate's word — 2026-07-26)
Laws before fires; bars before numbers. Every number below is PROPOSED
and hardens only on Bryce's countersign, before any loss curve exists.

## 1. Domain (the honest paragraph, both cases stated)
**KenKen-first (briefly), then arith3 — the lean.** For: every
instrument, fixture, meter, and clock lives on KenKen ground (the depth
ladder, per-depth labels, the flip-detector, the perturbation harness);
gate-opening and the revision signature can be proven on instrumented
ground in one short fire before any transfer risk is taken; the June
engine provides a same-domain comparison (the 3.5-breath clock) that
makes v200's clock READABLE against a baseline. Against: KenKen wins
transfer nothing by itself (the scope rivets are absolute), and the
campaign pays the calendar twice. **Arith3-from-birth.** For: the 257
and the misbinding species are the ONLY questions that matter to
December; every KenKen week is a week not spent on them. Against: a
failed arith3 cold-start is UNDIAGNOSABLE without the instruments —
we would not know if the architecture failed, the training failed, or
the gates never opened (today's specimen is precisely this ambiguity,
resolved only because KenKen fixtures existed). RULING SOUGHT:
KenKen-first capped at ONE fire with its own kill-bars; arith3
immediately after, carrying the proven recipe.

## 2. The gate-motion law (today's specimen converted)
The cold-start run moved what was easy (calib correlation 0.07->0.51)
and never found the gates (delta_gate at 0.5 init; waist_gate at 0).
Nothing in the objective ASKED for breath-differentiation.
- **The ask:** per-breath supervision (the v98 recipe's spine) — the
  weighted CE ladder is necessary but demonstrably not sufficient;
  add explicit per-breath IMPROVEMENT pressure (breath-k loss must
  beat breath-(k-1) on a margin schedule) so gradients must route
  through the gates.
- **Dashboard from step zero:** delta_gate excursion max_k
  |gate_k - 0.5|, waist_gate value, per-breath acc spread (breath-7
  minus breath-0) — printed every snapshot, never post-hoc.
- **EARLY-KILL (the cheapest bar):** if by 25% of the fire's step
  budget delta_gate excursion < 0.02 AND per-breath acc spread < 0.01,
  THE FIRE STOPS — it is manufacturing a second stack-wearing-
  recurrence. Pinned before step 0.

## 3. Budget in fires (proposed)
- **Fire 0 (smoke, ~500 steps):** gates move or die trying — the
  early-kill's own validation. KenKen easy band.
- **Fire 1 (KenKen, one fire):** kill-bars — gates open (excursion
  >= 0.05); revision signature PRESENT (flip events > 0 with w2r >
  r2w, corrections concentrated at deep cells); clock readable
  (per-depth t95 ladder exists); beats the frozen artifact's 0.410
  by >= 0.15 absolute. Any bar fails -> the arith3 fire does not
  inherit the recipe unexamined.
- **Fire 2 (arith3):** the decisive one. Kill-bars — recovery on the
  257 (the hard-fair fixture) STRICTLY ABOVE the 0.011 deterministic
  floor with margin: >= 0.10 (26+/257) to claim the mechanism;
  misbinding differential on the census species; ZERO new lies
  through the attestation fence (inherited bar, non-negotiable);
  revision signature present over arith3.

## 4. Fenced hypotheses (enter with measurements attached)
- **The joint law (carved):** sharpening at readout/interface, never
  in state. Design conformance reviewed at plan-freeze.
- **Spherical clause (interp 4):** direction-only commitment; its
  measurement is the rotation-geometry sub-read already attached to
  the revision-signature probe (rotation-on-sphere vs magnitude-
  collapse, same banked states).
- **Contrastive view (interp 8):** readout-side only; enters Fire 1
  as an ablation arm ONLY if base bars pass.
- All May-era components: see V200_CARRY_FORWARD.md — assumptions-
  to-be-tested, each with its cheapest refuting test.

## 5. Standing inheritances
Meter fixtures before any read banks. Conviction index grepped before
any claim banks. Bank-don't-read for every capture. The cannot-license
register retires only when a TRAINED model replaces the artifact.
Two-clock law governs all probe-vs-readout comparisons. Entourage
duty: the self-map re-probe seats at every promotion.

## 6. COUNTERSIGNED ADJUSTMENTS (Bryce, 2026-07-26 — inked; the plan is LAW)
- **Domain cap CONSTITUTIONAL:** KenKen-first capped at ONE fire; flip
  condition: Fire 1 passes -> no further KenKen fires under any
  rationale. PREMISE FLAG (reported at inking): v200's own validated
  instrumented ground as of today is its FACTOR-GRAPH domain (the
  cheap reads, flip detector, calib meter all ran there this
  afternoon); v200 has never had KenKen plumbing. Fire 0 therefore
  runs on v200's existing pipeline — the fastest diagnosable ground —
  with the KenKen-vs-factor-graph question for Fire 1 put to Bryce
  with this flag.
- **257-recovery bar, support arithmetic stated:** >= 0.10 = ~26/257
  vs the floor's 3/272; the read is BINOMIAL against p=0.011 (26/257
  gives p < 1e-20; even 10/257 gives p ~ 1e-6) — the bar is the
  MECHANISM claim (0.10), not bare significance. THE BAND PINNED:
  a print in (0.011, 0.10) is a REFUSED PROMOTION WITH ITS SCIENCE
  BANKED — above-floor-below-bar is a finding, never a negotiation.
- **Revision-signature-present, quantitative form (pinned before Fire
  1 exists):** flip-cells >= 2% of var-cells AND w2r/r2w >= 2.0 AND
  w2r events concentrated in the deepest tercile (rate ratio deep/
  shallow >= 1.5). Below any leg -> NOT PRESENT. Rotation-geometry
  rides, gates nothing.
- **SBP arm PROMOTED to Fire 1** (escape-valve may be load-bearing
  for gate-opening itself; the plan learns this before the decisive
  fire).
- **CALIB MARKED FALSE-FRIEND on the dashboard:** rising calib with
  flat gates is the SPECIMEN'S SIGNATURE — the early-kill's leading
  indicator, never progress evidence.
