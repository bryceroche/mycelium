# THE TOWER SPEC — multi-level IR above the macro floor
# (2026-08-12; spec-first; NOTHING here authorizes a fire)

**Summons (standing, gate long cleared):** the census's 83%
schema-altitude bar; the recursion charter (miner → rank-never-admit
→ book N+1 annotates one floor up); the macro floor BUILT (mg2,
macros expand before the solver sees anything — the key grades in
primitives, always). The tower = the levels above macro→primitive
that the charter promised and nothing has built.

## 1. THE GOVERNING LAW (this spec's whole difference)
The transfer science's directional form, carved the night before
this spec: **structure carries; training under structure consumes.**
The tower is the structural organ par excellence — grammars, typed
levels, expansion rules — and it inherits the receipts: macros
already transfer (they expand by construction; the solver never
sees them); the anchor carriage transferred; every learned-dynamics
mechanism did not. THE CLAUSE: **every tower level is a GRAMMAR
with an EXPANSION, never a learned behavior** — levels enter as
structure or they enter the graveyard.

## 2. THE OBJECT (what a level IS)
A level-k object = (grammar: typed factor classes over level-k−1
objects) + (expansion: deterministic rewrite to level-k−1, composing
down to primitives) + (gold derivation: level-k annotations derive
level-k−1 gold mechanically at mint — the E-floor's zero-cost-gold
lesson generalized). The parse may EMIT at any level; the solver
consumes primitives only (two-jaws, welded).

## 3. LEVEL-2 CANDIDATES (measured before designed)
The miner (schema_miner.py) runs its census on the CURRENT mix +
the 233's constituency: recurring subgraph classes ABOVE the mg2
macros — the deep rel-chains (the roster problems' shared shapes),
the ordinal-indirection schema (the tail's own family), the
percent/fraction pipelines. RANK-NEVER-ADMIT proposes; the
hand-quota is constitutional; one class pilots first.

## 4. FENCES (in from birth)
the key grades primitives ALWAYS (no certified object above level 0)
· macros-expand-before-the-solver generalizes to every level ·
rank-never-admit + hand-quota (the charter's rails) · the
criterion-key clause on every miner read · the wall-guard + both-
grains on any training arm · the December scope reservations.

## 5. ORDER OF WORK
this spec → THE MINER CENSUS (zero-GPU-class, on a word: what
level-2 classes recur, at what coverage, over the mix and the 233)
→ one-class pilot (grammar + expansion + derived gold; a book
tranche annotates one floor up per the charter) → the parse-side
emission fire (its own door, bars from the census's coverage
numbers). Patience is the schedule; the organs are the plan.
