# FIRE 2 DESIGN — THE FULL TRANSPLANT (countersigned ruling, 2026-07-27)
The last fire's job is to be DECISIVE, not parsimonious. Third and final
attempt; C4 stands at -0.05 unrenegotiated; three-strikes is
constitutional — no Fire 3.

## Diagnosis it answers
Dynamics dead (latent JSD 4e-4 -> 9e-5): dense pretrained layers act
near-identity on 32 ANONYMOUS latents — OOD inputs for token-trained
weights; the gate faithfully preserves a state nothing moves. Cheap
riders (gate init, THINK scaling) REFUSED by the mechanism itself:
scaling a near-identity map buys amplitude, not transformation. The
audit that reshaped the fire: the v98 spine's row was never fully
transplanted — supervision rode Fire 1; MARKERS and ALIGNED INIT did
not. The near-identity pathology and the missing aligned-init row are
plausibly the same fact.

## The three components (complete proven anatomy, administered once)
1. **Topological THINK mask** (#76): attention masked to factor
   topology, buildable at breath 0 on this domain (no compiler
   circularity here); soft temperature form per the attention-bootstrap
   law.
2. **Aligned init + markers** (the v98 spine's unshipped half, aimed at
   the diagnosed mechanism): latents initialized INSIDE the
   token-embedding manifold (in-distribution for the pretrained
   layers); per-breath markers retained/verified.
3. **Residual-primary restructure** (the state anatomy every measured
   ladder lived in): the loop's persistent state carried in residual
   form over the graph tokens rather than latent-primary — the
   largest patch of the campaign; call-graph law at FULL STRENGTH.
The depth-scheduled ask STAYS (correct, verified live, insufficient
alone). Improvement rider stays. Early-kill inherited. Dashboard full.

## THE THREE SIGNATURES (no patch trusted without its fingerprint,
verified at smoke IN THE COMPILED PATH, as Fire 1's CE wore its depth
structure):
- MASK -> attention-pattern SPARSITY printed from the compiled forward
  (masked-out fraction of attention mass ~0 where topology forbids).
- ALIGNED INIT -> breath-0 latent-to-embedding COSINE printed (in-manifold
  init reads high; anonymous init reads ~0 — the signature is the gap).
- RESIDUAL PATH -> its own GRADIENT-NORM group on the dashboard
  (nonzero from step 1; starvation = the two-terminal alarm).

## Pass-attribution, pinned BLIND before the fire
If C4 clears: the FIRST post-fire act is the ablation pair — mask-off
arm and anonymous-init arm — so the finding enters the record with its
mechanism ASSIGNED, never its conjunction celebrated. Authorization
never required attribution; the record does.

## Both endings, written now
- C4 clears -> the bet lives; ablations assign mechanism; arith3
  planning resumes under the gate's original bars (257-recovery,
  zero-new-lies).
- C4 fails wearing every organ the ladder-forming lineage ever had ->
  the sentence is airtight: THIS ARCHITECTURE AT THIS SCALE REFUSES
  THE LADDER EVEN WITH THE COMPLETE PROVEN ANATOMY; the trichotomy
  reopens with the examination genuinely complete, not
  budget-exhausted.
