# CAMBIUM — the vision, named (2026-08-03)

*(Gut #124's design, one page. The cambium is the living sheet where
the whole tree grows AT ONCE; what it lays down is RINGS — a committed
record, strictly ordered, readable forever. One organ, two regimes:
growth parallel in dynamics, record sequential in artifact. The tree
grows on the mycelium's network.)*

## The design

- **The cambium (replace-notebook):** the working state — per-breath
  512d waist snapshots refining at ALL altitudes simultaneously.
  Signature C's measured behavior. Today's loop has only this, and
  destructively (state overwritten per pass — why bindings erode).
- **The rings (accumulate-notebook):** the committed record — where
  the pawl clicks, hard and ordered, and where a LATER constraint can
  cross a commitment out (rung 3's reversible commitments; the
  ratchet law's designed engagement point; the substrate verification
  reads). Both notebooks live per breath cycle.
- **The tower (explicit IR floors):** sequential in ARTIFACT by
  construction — level N+1 consumes what level N committed; the key
  grades in primitives, always. The tower and the loop are separate
  axes, separated BY the notebook split. Seven floors is gut #123's
  prior; the error-altitude census gates floor three.

## What is MEASURED (vintage attached — the never-mix rivet)

| Evidence | What it says | Vintage/scope |
|---|---|---|
| Signature C (crossover probe, 2026-07-26) | all altitudes refine simultaneously; no handoff; schema never erased; t95 ordered by altitude | **KenKen June engine ONLY** |
| K-sweep retro-read (artifact 2026-07-26, ledgered 2026-08-03) | solve t95 by depth 1/3/8; cut-to-K8 cost 0.5%/0.6%/4.2% — deeper degrades FIRST, 8x | same engine; C's spectral mechanism at the solve grain |
| Depth-parallel deduction (June) | K_min ~ D/4 — logical depth parallelizes across breaths | June engine |
| SBP sigma=0.02 (+0.0153) | noise load-bearing for basin escape — fixed-point iterator behaving like a denoiser | June engine |
| Engagement-point read (gen-22 era) | the parser's pawl clicks early and never releases — one-pass CANNOT revise | the deployed parser (the problem statement) |
| v24c dual notebook (May) | working memory across breaths, validated in its era | resting ancestor |

## What is CONJECTURE (enters by measurement, never analogy)

- The parser inherits C's dynamics (the v200 capture never ran on a
  trained model).
- Seven floors (#123's prior; prediction banked against the tower's
  measured final depth).
- The dual notebook cures binding erosion (rung 3's bench question).

## The road (in the standing order)

1. ~~census~~ READ (schema floor PAYS for the reachable class). **The
   floor has TWO faces (#130): span-marking on the input side (mark
   constraint-bearing spans, vent narrative water — the tarpon's
   vacuum inhale) feeding copy-not-generate's pointer targets on the
   emission side. FIVE arrivals (census/tarpon/copy-not-generate/
   compliance/lionfish) — the floor's job at its sharpest (#135):
   make partially-reachable look different from truly-foreign.
   Spec-stage behind the wide word; #126: own generation.**
2. Rung 3's word: the dual-notebook mechanism is the bench's candidate
   design (R2 reversible commitments = the rings).
3. Frame territory beyond KenKen: by measurement on the parser
   lineage, never by analogy (the #71 rent clause).
4. ~~The canonicalizer~~ BUILT+PASSED 2026-08-03 (mycelium/canonicalizer.py routes identity through the knot-ID door; false-merge 0.000000 on 35,876 gold rows, invariance 1000/1000; paraphrase-collapse read PENDING-GPU).

**Fences carried:** waist snapshots are notebook SUBSTRATE, never the
admission signal (the mouth reads frozen trunk space — rotation law +
Goodhart fence). 'Diffusion-like dynamics' earned; 'diffusion model'
never. The key grades in primitives at every floor, forever.
