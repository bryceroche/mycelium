# BREATHCYCLE_SHUFFLE_SPEC — the Grand Vision's organ (2026-08-15)

**Form:** iterated propose/dispose across settle cycles. Round r: forward
under current route-masks -> if still refusing, RE-MATCH from round-r
outputs (one settle cleaner than round r-1 — the disposal's signal
iterates toward a fixed point instead of trusting the collapsed pass-1;
A0's grave answered by construction). FDM: six antiphase carriers as the
matching's phase-separation cost (adjacent slots avoid mod-6 collision).
TDM: R rounds, capacity 6R. Trigger: refusal only. Wiring imposed; no
gates; no loss terms before any trained rung.

**THE FOUR ARMS (attribution law — separate receipts or separate graves):**
- TDM-only: iterated re-matching, plain order fallback
- FDM-only: single round, phase-cost fallback
- COMPOSED: iterated + phase-cost
- PLACEBO: iterated with per-round rotated (wrong) routes

**BC0 BARS (pinned pre-fire):** solved hold >=0.98 every arm; the ONLY
admissible yield meter = ARM CONVERTS MINUS PLACEBO CONVERTS on the
residue-74 (differential >=4 = routing is real; <4 = tremor, arm dies);
per-arm identities banked. Zero training. Training rungs exist only
behind a BC0 differential receipt.
