# THE NAZARÉ OPERATOR SPEC — commitment where the wave breaks
# (2026-08-12; spec-first; NOTHING here authorizes a fire)

**The three pieces, measured (the spec's whole foundation):**
the ACT — rebinding (args move; nothing lights, nothing retypes);
the FIELD — decode-grade rebinding events, dup-routed: rebound
slot contained 15/15 at median 3-sparse (perfect recall, 1-in-3
precision); the BOUND — where, not whether (row-level AUC ~0.5:
the field locates the act, never grades it).

## 1. THE RECOGNITION (v1 exists)
The row-gated cross-grain anchor IS the operator's first form:
commitment (whole-bank anchoring, ensemble-consistent) exactly
where the event field is non-empty — measured at nd4 5/15
carriage, 0.7% passer cost. **Nazaré-v1 = the row gate, renamed
by its mechanism.** Its numbers are the operator's floor.

## 2. THE OPEN FORMS (each a question, none assumed)
- **(B) event-focused breathing:** authority concentrated at event
  slots WITHIN the loop (per-slot gate modulation). The ensemble
  law was measured at OUTPUT-STATE blending; within-loop gate
  modulation is a DIFFERENT SITE — unmeasured. Its smoke question:
  does per-slot authority inside the loop inherit the mixing
  poison or escape it? (One inference read decides the site's
  lawfulness before any design.)
- **(C) wave-ordered commitment:** the event field as a SCHEDULE
  (event slots' sentences re-read first / committed first).
  Spec-stage; the boulevard's refutation fences any
  field-reduction framing — ordering must ADD, not remove.

## 3. FENCES (in from birth)
the ensemble law (no per-slot OUTPUT mixing — v1's own carriage
receipt) · where-not-whether (the field never grades; any use of
event-count as a quality signal is the bound's violation) ·
Goodhart (events as inference-time ROUTING = lawful data-flow,
like the dup bit; events in any LOSS = concealment training —
never) · the two-read organism scope (any engaged deploy tier =
the December question, reserved, never absorbed) · the wall-guard
and both-grains-from-birth on any training arm.

## 4. ORDER OF WORK
this spec → the (B)-site lawfulness smoke (inference-only, one
read, on a word) → the surviving form's design registration
(numbers from v1's floor) → fire. Patience is the schedule; the
organs are the plan.
