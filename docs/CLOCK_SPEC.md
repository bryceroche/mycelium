# THE CLOCK SPEC — organ 3, the WHEN organ (2026-08-08; spec-first;
# nothing here authorizes a build or a fire)

**Summons (three, all measured):** the dup cure's double-failure
branch pre-wrote it; arm 1 flipped 15/15 PREMATURE→LOST (a cure that
moves timing exposes the next layer); the beam exit cannot reach WHEN
(capacity tool only). The premature-commitment family (mis-anchoring
spatial + dup argumental — one root, two grains) is the disease; the
ratchet law (#94: engagement MEASURED, never emergent) is the charter.

## 1. What the clock is
The commitment-timing organ: it decides WHEN a slot's binding commits
(the pawl's designed click), so that commits form AFTER their
evidence arrives (dup's operand, the anchor's home) instead of at
tok-21-for-a-tok-23 operand. It does not decide WHAT binds (heads),
WHERE it binds (anchors), or WHETHER it certifies (the chain).

## 2. The Nazaré frame (hypothesis, operator unchosen)
The wave is a FOCUSING wave, not a periodic one: graph structure =
bathymetry, evidence = swell, commit = the break where the medium
narrows. THE FIRST DESIGN QUESTION (pinned before the image can
flatter itself): the operator — what quantity IS concentrated
evidence-energy? Candidates, each measurable on banked parses:
  (a) incoming constraint count at the slot
  (b) the slot's degree in the emerging graph
  (c) the fraction of the slot's arguments already bound
  (d) sentence-completion of the slot's own span (the operand-
      complete signal arm 1 aimed attention at)
**The operator scan precedes any build**: which best separates
healthy commits (nd=0 correct rows) from premature ones (the tok-21
rows)? Chosen by measurement. **THE NULL BRANCH (pinned): four is
the registered set — if none separates, concentration is not the
operator and the Nazaré frame owes a DIFFERENT design principle;
no fifth candidate invented at the scan.** Correlated candidates
(a/b/c rise together as context fills) are distinguished from
proxies by pairwise correlation, reported with the scan.
**SCAN VERDICT (2026-08-08): (d) SELECTED** — own-sentence
completion, AUC 1.000 (healthy 1.0 vs premature 0.167); (a) also
1.000 but IS (d) in disguise on this fixture (the 4th visible
mention IS the operand — perfectly correlated, the proxy caught by
the rider); (b) flat; (c) uninformative-by-saturation (text order
binds the given before every rel — unrefuted, untested). Clock v1
gates commitment on OWN-SENTENCE COMPLETION.

## 3. THE FENCES (all inherited, in from birth)
- **Register clause:** the commit trigger is its own trained head
  with its own gold — solver-verified finality per breath ("this
  binding, at this breath, was final"). Settle is NEVER a component.
  **Cross-breath convergence is GOLD-SIDE ONLY** (finality labels
  derived at train time) — the same quantity read at inference to
  decide anything = settle-as-component = refused. Written before
  the operator scan runs, because the scan will be tempted.
- **Phase fence (the cathedral's three failures):** the wave touches
  MEMORY/COMMIT ordering only, never the deduction path.
- **Goodhart fence:** no diagnostic in any loss; assert_not_
  supervised at the door.
- **Neighbor-cost law:** the fire's battery includes neighbor reads
  (nd=0 dup, bigtest, anchors) — what does the clock break that
  nobody looked at?
- **Stacked-pathology law:** verification at mechanism grain — the
  timing read (HELD AND WON), never score-only.
- **What's-held-stationary screen:** the clock holds the ANCHOR
  frame fixed and varies commit TIME against it (the nut is the
  anchor; the screw is the pawl).

## 4. First customer
The three-layer dup cure: STAGE (this organ) + WIN (preference
supervision at the operand-complete moment, weight ≪1.0) + EXIT
(the beam exit, kept). Bars inherited: nd=1 15/15→<8/15; held-and-
won at token grain; no-harm nd=0 ≥10/15; bigtest −5; kill >5.
Second customer (unpriced): the filler species — same root, spatial
grain; if staging cures argumental prematurity, the spatial form is
the natural next read.

## 5. Order of work (the plan's Phase 1)
spec (this) → operator scan (banked parses, zero-build) → clock v1
(rings machinery, one cut, init-closed) → fire through the door on
inherited bars. Patience is the schedule; the organs are the plan.
