# CORE_COMPONENTS — the conviction board (2026-08-19)
Status: RECEIPT (controlled+replicated) / WIN (controlled, replication owed) /
SPEC (designed, unfired) / VINTAGE (era-tagged motivation) / GRAVE (closed).
Every claim cites the ledger; bigtest certifies; placebo law governs.

## TIER 1 — RECEIPTS (build on these)
1. **SIX HELICAL WAVES** — the coherent-phase seed: 2.4x steps-to-competence,
   placebo-proven on TWO channels (sent_emb RECEIPT; breath_emb WIN, 227 owed).
   Coherence is the cause (matched-energy scrambles inert, twice). The shuffle's
   living form is TEXT-SPACE (len_asc 159/233, gate passed, UNDEPLOYED).
   Refine: seed-227 for channel 2. Extend: sephase-Q (slot banks), waist_w.
   Titrate: one channel per arm; composed only behind two solo wins.
12. **GROUPER'S JAWS** — neural proposes, symbolic disposes: the constitution.
   This week's refinement: the disposal needs an INDEPENDENT signal (A0's law).
   The key gates all data; the mouth-vote-panel-key chain never lies.
3. **CATHEDRAL + NOTEBOOK** — ADDITIVE-OPTIONAL is the lawful memory form:
   cell-2 23/58 vs 11/40 (first optional structure ever engaged; two-phone law
   refined: used because it relieves the MEASURED saturation cap, ladder dead
   past breath 3). Graves: imposed roadcuts (ensemble law). Refine: focal read
   (magnifying glass), echo/flutter inks. Owed: maturity + bigtest cert.
4. **512 WAIST / DANCER'S SILHOUETTE** — inline form load-bearing since July
   (envelope kept, carrier shed); side-channel form = the notebook's W_sil ink.
   Extend: the ink variants ARE the silhouette program now.
5. **ANNA KARENINA / FINGERPOST** — multiple views -> whole truth: unanimity
   1.0000; engineered views union 31->17, vote 255/255 zero-wrong; len_asc.
   THE DECEMBER DELIVERABLE: deploy len_asc + engineered set into the TTA
   organ (gate PASSED, word owed — the calendar's head).

## TIER 2 — WINS AND LIVE SPECS
2. **TRAFFIC CIRCLE** — the principle stands (waist/mouth/key ARE circles:
   no bypass); the state-replacing roadcuts died (ensemble law). Live forms:
   query-side circle, rotating focal read (registered, unfired).
6. **MLIR TOWER** — CHAIN_MUL/mg3 admitted (sight 100/100); the recursion
   charter (books one floor up) is the diffusion-compiler form. Extend: next
   macro class via schema miner + book campaign. The binding war gated it.
7. **NAZARÉ WAVE** — focused-wave receipt (events 1.0/bg 0.05, bill halved);
   BOLT-ON deployment form stands; training-integration is #55's grave.
   Titrate: onto the from-birth lineage at maturity, inference-only.
9. **MASKING + REGISTRY INTERFACE** — masking constitutional (wiring law,
   auto mask-prep); registry = the ftype alphabet + rank-never-admit door.
   Refine: lsent-aware mask-prep (banked fix), mid-phase re-prep (charter 1).
   POINCARÉ BALL: parked behind its flag (the K7+notebook ladder RISES, so
   the depth-geometry trigger is unmet; marriage clause standing).

## TIER 3 — VINTAGE / RETIRED (motivation, not evidence)
8. **ALTERNATION NL<->SOLVER** — first delivery died clean (0/32, placebo'd);
   the 43%-derivable asset + query-var partial-answer road banked.
10. **PERCEIVER CONDUCTOR** — retired as core BY RULING; monitor/segmenter
   only. Revival needs new theory + word.
11. **ITERATIVE PREFILL** — May receipt, June-engine substrate; vintage.

## THE TITRATION QUEUE (one delta at a time, from here)
227-replication(breath seed) -> gsb_real maturity + BIGTEST cert ->
sephase-Q -> focal read -> Nazaré bolt-on -> echo/flutter inks.
THE DEPLOYMENT WORD (item 5) rides above the queue: December's deliverable.

## TIER 3 REVIVAL ROADS (2026-08-19 — the misunderstood, given their due)
- ALTERNATION's unfired message type: REFUTATION — the solver's minimal
  conflict set names the parser's suspect factors (where-wrong, not what's-
  true; the dead delivery sent knowns, which duplicated success). Revival:
  conflict-guided re-read, refusal-triggered, random-accusation placebo.
- CONDUCTOR's unfired role: the system is an orchestra with no conductor —
  organs with scattered triggers (refusal/heat/claims/dissent). The
  SANCTIONED monitor role = inference-only scheduling: read diagnostics
  (never in loss), route rows to organs. Scheduling, not compute.
- PREFILL's unfired composition: FINGERPOST x PREFILL — the vote's
  UNANIMOUS factors (1.0000 tier) explicitated as text, re-read only the
  disagreed region. Certified ink where the dead delivery used uncertified.
Tier-2 correction: the tower is NOT parked — form_mix8 trains every
from-birth arm daily; only its next floor waits. Tier-1's love =
protection: bigtest certification + the deployment word.
