# V200 CARRY-FORWARD INVENTORY (draft — 2026-07-26)
Law: unexamined inventory is where revenants breed. Every component
enters as an ASSUMPTION-TO-BE-TESTED: proven lineage stated, transfer
assumption named, cheapest refuting test attached.

| Component | Proven on (lineage) | Transfer assumption | Cheapest refuting test |
|---|---|---|---|
| Per-breath supervision (CE ladder) | v98 Sudoku/KenKen recipe (engine lineage) | gradient routing works at H=2048 latent-primary | Fire 0: ladder spread appears by 500 steps |
| Aligned embed<->codebook init | v98 recipe | alignment matters for latent (not residual) primary state | Fire 0 ablation: aligned vs random codebook init |
| Verification inlet (LIVE at init) | engine lineage (KenKen oracle parity) | inlet helps when latents, not cells, carry state | Fire 1 ablation arm, only if base passes |
| delta_gate convex blend | engine lineage; TODAY'S SPECIMEN: never opens under pure CE ladder | needs explicit per-breath improvement pressure | the early-kill IS the test (step-budget bar) |
| Alternating waist / compress-expand | v38/B-field era (OLD residual lineage) + v200 cold-start (gate never opened) | compression helps latent-primary state | Fire 1: waist_gate motion + ablation arm |
| Q rotation / per-head pitch | May era (v22/v23, OLD lineage; rotation was the ONLY load-bearing component in ablations) | rotation transfers to cross-attend READ | Fire 1 ablation arm; cheap |
| Calibration stepping (Dopri5-style) | engine calib head (breath-0 verdict vocabulary measured) | calib can gate adaptive depth on v200 | calib-vs-correct AUC curve (instrument already validated today, 0.07->0.51 on the frozen artifact) |
| SBP noise sigma=0.02 | v6-era (+0.0153) + escape-valve principle | noise still helps a latent-primary loop | Fire 1 ablation arm; #74's fences apply |
| Tree codebook readout | v200 cold-start itself (only surviving trained piece with signal: calib channel) | digits-MSD readout adequate for arith3 values | Fire 2's own bars |
| Topology tensor / structural masks | engine lineage (factor topology = attention masks) | arith3 graphs' topology expressible in READ masks | paper design review at plan-freeze + Fire 2 |
| Mirror staging / photon oscillation | SPEC-STAGE ONLY (never validated anywhere) | — | DOES NOT ENTER; re-registers from scratch if ever |
| BAU-shaped training plan (gut #85): interleaved diet + inheritance-respecting LR + active ramp leash | NEGATIVE lineage, measured: three fires + re-light were cold-start narrow-objective hammering; the re-light DESTROYED the pretrained polarization ramp (2.28σ→0.78σ) en route to the wrong attractor. POSITIVE lineage: both promotions came from gentle continuation on interleaved diets | catastrophic interference is the common cause upstream of all three poles — a BAU-shaped plan prevents inheritance destruction on any future loop | ramp-preservation metric LIVE on the dashboard (layer-separation printed per N steps; a loop that eats its ramp is killed for it); the leash's ACTIVE form (KL-or-cosine anchor to the pretrained reference) enters as an ablation arm |
