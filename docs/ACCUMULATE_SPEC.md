# THE ACCUMULATE SPEC — the notebook's surviving half
# (2026-08-11; spec-first; NOTHING here authorizes a fire)

**Summons (measured, inherited whole):** door #54/#54-R closed
persistence-within-the-pass BY MECHANISM (shy and open pawls, churn
108~111 identical). The notebook's case passed here entire. The
constituency: the deep cells (dial-independent at every coin
weight) and the engaged grain's churn. The dowry: the mussel's
shape (transform-and-return), the cube's clause (return to SHAPE,
not rest), commit-on-change, the certification fence (transform
lawful upstream, never at the mouth), and twice-trained commit
machinery.

## 1. THE TRANSFER QUESTION FIRST (the charter pattern governs)
Three components engaged; three claimed mechanisms failed to
transfer (breaths at authority; the pawl shy; the pawl open). The
shared anatomy of the failures: **each mechanism operated WITHIN
the trained loop's dynamics** — asking learned weights to use a
capability the loss never made load-bearing. THE SPEC'S CENTRAL
CLAUSE: any accumulate design must state, before its fire, WHY its
mechanism transfers where those did not. The available argument,
and the only one with receipts: **STRUCTURE TRANSFERS BY
CONSTRUCTION; LEARNED DYNAMICS DON'T** — the pointer law's family
(binding enters as structure: masks, spans, letters; W_dargs'
single-target gold; the era/sha fences). An accumulate that
persists state ACROSS reads architecturally — state re-entry the
forward cannot ignore — inherits that receipt. An accumulate that
asks the loop to learn to persist inherits the pattern's three
corpses.

## 2. THE OBJECT DECISION (the first design fork, undecided)
What accumulates, across what boundary:
- **(A) State-side anchors:** committed slot states from read k
  pinned as anchors in read k+1's forward (the anchor machinery
  exists; re-entry structural). Grain vocabulary: pass-1 vs pass-k
  replaces silent-vs-engaged.
- **(B) Decode-side conditioning:** pass-1's high-confidence
  factors re-enter pass 2 as conditioning (the committed partial
  as ADDED context — the congestion refutation FENCES any
  field-reduction claim; addition only).
- **(C) The June form:** replace + accumulate as dual notebooks
  within an episode (v24c's 96/94/91 receipt — but that receipt is
  vintage-riveted to the June engine; inheritance by measurement
  only).
The fork resolves by a cheap read: does a pass-2 forward
conditioned on pass-1's committed factors change the deep cells at
all? (An inference-only smoke, no training — the mask read's
lawful sibling on the ADDITIVE side.)

## 3. FENCES (in from birth)
teaching-not-telling (supervision-side; inference self-conditioning
is a design question, not automatically licensed — ruled at the
object decision) · the certification boundary (accumulate output
NEVER feeds the mouth's admission or the key's grading; the key
grades primitives, always) · the cube (cold-start SHAPED; the
notebook empties to canonical form) · commit-on-change · Goodhart
(no diagnostic in loss) · the wall-guard + both-grains-from-birth
(in their pass-k vocabulary) · the 2-of-3 goalpost rider (score
moves only by registered scope change).

## 4. BARS (numbers at fire-registration, shapes now)
The deep cells move (the constituency's own read, set-based
instruments); cross-read churn CUT (the pawl's failed primary,
restated at the pass grain); the transfer clause's own
verification: the mechanism's effect must survive ITS OWN removal
test (remove the re-entry, the effect must vanish — proving the
structure carried it, not relearned dynamics).

## 5. ORDER OF WORK
this spec → the object-decision smoke (§2, inference-only,
zero-training — fires on a word) → the chosen form's design
registration (bars numbered, both branches pre-written) → fire.
Patience is the schedule; the organs are the plan.
