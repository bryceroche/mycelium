Pls read this with @Claude.md in mind.

Create a github release, minor release and then rip out non-essentials we want bare-bones

Core problem 1 aka the scorpion: lexical vs operational similarity of embedding clusters
Core problem 2 aka the octopus:  DAG step requires further decomposition by LLM
Core problem 3 aka the gorilla:  tree leaf nodes require further decomposition
Core problem 4 aka the snake: big bang expansion during training 

Constraint 1 aka the Fox:  Each pass of tree results in 1 leaf node activated
Constraint 2 aka the Cat:  add wave function to the project
Constraint 3 aka the Dog:  add lessons from AlphaGo paper 


DAG_Step (table)
 - Dag_step_id (PK)
 - Step_desc
 - Is_atomic
 - Step_num (1…n)
 - Branch_num (1..n)


Thread_id (Table)
 - Thread_id (PK)
 - Success (0 or 1)


DAG (table)
 - Dag_id (PK)
 - Success (0 or 1)
 - Benchmark (gsm8k, math500 L1- L5 etc)
 - Difficulty_level
 - Problem_id
 - Problem_desc


Thread_step (fact table – used for post mortem analysis MCTS rollouts)
 - Thread_step_id (PK)
 - Node_id (FK)
 - Dag_step_id (FK)
 - Thread_id (FK)
 - Dag_id (FK)

If there are several steps to the problem and we are picking several leaf nodes per step then we have substantial number of threads per problem is this okay?  

Llama— mcts rollouts only pick the top n leaf nodes for each step. Thread id essential for back propagation.  Post mortem credit propagation.  What does this look like?  We have thread ids showing different paths with dag steps and leaf node combinations that succeed or failed. The post mortem needs the dag step node combination 

Llama is our main signal and should guide all decomposition dag step or leaf node.  Do not decompose leaf node until instructed by llama.  This post mortem analysis should consider confidence at each step, depth level of leaf node and count of node hits count of node success and dag step associated with each node other statistics.  Do we rerun failed problems after the post mortem?  We need to confirm that we are collecting all these statistics in our database and that they are current. We don’t want to decompose nodes with good statistics 


Are we doing a good job guiding the llm with the DAG plan?  We should be clear that we want simple atomic steps. Each step should not be capable of being broken down further.  We need to emphasize this 

Relax constraints on centroid movement
Centroid updates on post mortem 
We need a brainstorm how do we cleanly merge centroids that are too close

Do we process the postmortem analysis in batches?

Alpha go policy and value network with mcts 
We might need confidence scores
MCTS rollouts – you dont need to branch on every dag step only the ones where undecided.  Maybe take the top two
Post Mortem Analysis - is this batch processing or after each problem?

Increase Claude budget 
Incorporate wave into system 
Alpha go inspiration 
Learn from success and failure 
Parameters names must be generic 

Multi step sensor that flags dag steps that are too complex 
What do leaf nodes look like how do we know if they are working or need decomposition or if dag step needs decomposition 

 

Are we happy with the natural language interface between the node and the llm? 

Dolphin— leaf node fails because of bad upstream routing 
Butterfly— dag step ground truth? 
Raven— How to spot bad dag steps or plans 
Zebra— when to stop leaf node decomposition
Grasshopper — assembly of dag step results 


If a leaf node is being used successfully many times — zebra 
Cat— propagation of credit +/-
Mouse— how do you know which step failed 





How do you know if it’s the octopus or the gorilla and by that I mean, how do you know if a dag step is too complex or if the signature DSL is too generic?

Addressing the Octopus - DAG Step Decomposition
The Octopus violates the constraint applied by the Fox. The DAG plan generated by the LLM might include multiple steps.  Further decomposition is required because the tree can only route to one leaf node per pass (Fox constraint)

Detecting the octopus.  If we are undecided about which fork to choose close to the root.  Might be a little tricky to balance this with early stages of training when all nodes are close to the root.  Lets brainstorm this.   


Addressing the Scorpion — Max MCTS signal 
Scorpion problem: We don’t have ground truth for each DAG step only the final answer
Scorpion: Added thread_id and running multiple threads per problem to tease out which steps are working.  Using MCTS rollouts.


During training we want to be using MCTS rollouts to sample multiple high probability leaf nodes propagating signal on success or failure.  Idea - let’s add a confidence metric that is factored into the signal propagation.  High success and failure = strong negative signal.  We are currently only propagating positive signals I think - but we want to also learn from failures.  


Gorilla -
Independent of the Fox constraint.  The leaf node is too complex and requires further decomposition only if all the sampled MCTS rollouts fail.


Snake: Big Bang Function Adjustment
Disable the existing big bang method and instead allow the tree itself to learn and grow.  So if there are failing leaf nodes then decompose leaf node or DAG step. 



Needs to be continuous and it might not be today

Solveit Discord channel – Launch
Claude discord channel 
Or any active discord channel



Brainstorm forking at higher levels in the tree for harder problems – big bang function – ideally this is a smooth and continuous function.


